#define MAX_BOUNCES 128             // Maximum bounces before a ray is forcibly discarded
#define EPSILON 0.0001              // Epsilon parameter to avoid collisions to the same mesh
#define MAX_BVH_DEPTH 64            // Maximum depth of BVH
#define SUBDIVISIONS_PER_AXIS 2     // Used for multisampling, square gives the number of subpixels